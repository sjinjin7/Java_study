<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ksj.mapper.BoardMapper">
	<select id="boardList" resultType="com.ksj.model.BoardVO">
	
		select * from board;
	
	</select>
	
	<select id="boardListPage" resultType="com.ksj.model.BoardVO">
	<![CDATA[
	
		select number, subject, name, DateInserted, cnt 
		from(select @rownum:=@rownum+1 as rownum, number, subject, name, DateInserted, cnt from (select @rownum:=0) as rownum, board order by number desc) board 
		where rownum >=#{pageNum} and rownum<=#{amount} ;
	]]>
	
		
	
	</select>
	
	
	<select id="boardDetail" resultType="com.ksj.model.BoardVO">
	
		select * from board where number = #{number};
	
	</select>
	<insert id="boardWrite">
		insert into board(subject, name, content, cnt) 
		values(#{subject},#{name}, #{content}, #{cnt})
	</insert>
	<update id="boardModify">
		update board set subject = #{subject}
		, content = #{content} 
		where number = #{number};
	</update>
	<delete id="boardDel">
		delete from board where number = #{number};
	</delete>
	
</mapper>